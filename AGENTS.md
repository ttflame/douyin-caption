# 业务设计
- 业务逻辑梳理，优先使用已有且成熟的的业务逻辑设计经验
- 在新业务和创新型业务逻辑，需要理清楚业务的逻辑，业务流程，可采用抽象类比等手法进行设计

# 开发技术技术选型
- 按用户具体需求来决定
## 桌面
- tauri + vue3
- electron + vue3
- flutter
- 其他桌面开发组合
## web开发
- golang + vue3
- fastapi + vue3
- 注意：Python 必须创建虚拟环境进行开发
## 小程序
- 微信使用微信小程序开发推荐的技术选型
## 人工智能（本地模型及算法）
- 开发人工智能推荐使用Python或者node 或者两者搭配使用，主要用于后端，如ai大模型部署调用
- 注意：Python 必须创建虚拟环境进行开发

# 开发规范
- 让代码更容易去承载复杂的业务逻辑，使代码简简洁易懂，具有高内聚、低耦合特性，能够容易扩展、更新。
## 开发原则solid
- 规划清晰的项目目录结构，为了提高扩展性，每个层都要有对应的文件目录
- 严格遵守单一职责、开放封闭原则、依赖倒置原则、里氏替换原则、接口隔离原则、迪米特原则、合成复用原则
- 设计模式（Design Patterns - Elements of Reusable Object-Oriented Software）
## 项目修改
- 不能有旧代码遗留
- 修改代码时进行关联性检查，有关联进行就行关联性修改
## 开发日志
- 简单记录新增、修改、删除逻辑，包含时间等关键信息记录，简单易读。
- 保存在项目根目录下，保存.log文件。
## 禁止
- 不能为了实现而不遵守开发规范

# UI设计规范
- 目标是为了用户视觉上的舒适和减少认知负担
- 要求自适应、弹性化布局
- 按板块、功能等进行划分区域，比例均匀
## 排版
- 排版遵守：对比、对齐、重复、亲密性原则
- 禁止堆叠功能，简化操作流程，减少用户认知负担
- 使用菜单页面、弹窗、折叠等方式收纳次要及不常用的功能
## 布局
## 容器、盒子、box、UI组件
- 自定义ui组件，减少系统组件的使用。界定范围：表单组件、按钮组件
- 采用圆角
- 减少容器边框的使用，大致范围界定例如：文本框、下拉选择框是需要边框的；容器、按钮等一般不需要边框。
- 容器加轻微阴影
- 在需要使用滚动条的容器（如多内容的情况）鼠标划入时才显示滚动条，滚动条预留一定的空间，避免划入时页面抖动。
- 滚动条不能使用默认的滚动条样式，优先是细滚动条，淡色。
## 文字
- 禁止使用粗体

## 配色
- 布局配色主要用于能够识别区域
- 状态色采用中性色
## 动效
- 动效主要用于用于的操作状态的提示，动画要流畅，不能过于生硬
- 每个组件应当都有动效，如状态改变、鼠标划过等
- 动画空间预留、不能使页面有跳动感、保持平滑过渡

## 图标
- 禁止使用字符来代替图标