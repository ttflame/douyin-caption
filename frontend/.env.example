# Leave VITE_API_BASE_URL unset for same-origin production and the local Vite proxy.
VITE_API_BASE_URL=/api/v1
VITE_BACKEND_TARGET=http://127.0.0.1:8000
# Optional local-development login defaults. Never commit a real password.
VITE_DEFAULT_LOGIN_USERNAME=
VITE_DEFAULT_LOGIN_PASSWORD=
